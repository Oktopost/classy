/*'use strict';


const Singleton = require('../../index').Singleton;
const assert = require('chai').assert;

console.log(require('../../index'));


suite('Singleton', () => {
	test('Object is returned', () => {
		assert.isObject(new Singleton({}));
	});
});*/